using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AttackScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /* Prawy przycisk, jak ray to screen trafi object to trzeba sprawdzic jaki tag lub layer ma ten object i na tej podstawie wywolanie konkretnej akcji
     * 
     * 
     */
}
